<UserControl
    x:Class="TunrRT.LibraryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TunrRT"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="400">
	<UserControl.Resources>
		<Style x:Key="LibraryHeaderStyle" TargetType="TextBlock">
			<Setter Property="FontFamily" Value="Segoe WP"/>
			<Setter Property="FontSize" Value="48"/>
			<Setter Property="FontWeight" Value="Light"/>
			<Setter Property="TextWrapping" Value="NoWrap"/>
			<Setter Property="Foreground" Value="Black"/>
			<Setter Property="TextLineBounds" Value="Tight"/>
		</Style>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"></RowDefinition>
			<RowDefinition></RowDefinition>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0">
			<TextBlock Text="music" Style="{StaticResource LibraryHeaderStyle}" FontSize="82" />
			<ItemsControl>
				<TextBlock Text="{Binding Text}" Style="{StaticResource LibraryHeaderStyle}" />
			</ItemsControl>
		</StackPanel>
		<ItemsControl Grid.Row="1">
			<Canvas>
				<ListBox VerticalAlignment="Stretch">
					
				</ListBox>
			</Canvas>
		</ItemsControl>
	</Grid>
</UserControl>
