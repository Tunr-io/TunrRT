<UserControl
    x:Class="TunrRT.UserControls.LibraryListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TunrRT.UserControls"
	xmlns:data="using:TunrRT.DataModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="480"
	d:DataContext="{Binding Source={d:DesignData Source=/DataModel/SampleData/SampleData.json, Type=data:DataSource}, Path=BrowseLists[0]}">
	<UserControl.Resources>
		<DataTemplate x:Key="ArtistListTemplate">
			<local:ArtistListControl DataContext="{Binding}" />
		</DataTemplate>
		<DataTemplate x:Key="AlbumListTemplate">
			<local:AlbumListControl DataContext="{Binding}" />
		</DataTemplate>
		<DataTemplate x:Key="TitleListTemplate">
			<local:TitleListControl DataContext="{Binding}" />
		</DataTemplate>
		<local:LibraryListTemplateSelector x:Key="LibraryListTemplateSelector" 
										   
										   ArtistListTemplate="{StaticResource ArtistListTemplate}"
										   AlbumListTemplate="{StaticResource AlbumListTemplate}" 
										   TitleListTemplate="{StaticResource TitleListTemplate}"/>
	</UserControl.Resources>
	<ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource LibraryListTemplateSelector}" />
</UserControl>
