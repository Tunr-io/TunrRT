<UserControl
    x:Class="TunrRT.UserControls.AlbumListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TunrRT.UserControls"
	xmlns:data="using:TunrRT.DataModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
	d:DataContext="{d:DesignInstance Type=data:SampleAlbumLibraryList, IsDesignTimeCreatable=True}">

	<UserControl.Resources>
		<local:AlbumListConverter x:Key="AlbumListConverter" />
	</UserControl.Resources>

	<GridView IsItemClickEnabled="True" ItemsSource="{Binding Results, Converter={StaticResource AlbumListConverter}}" ItemClick="GridView_ItemClick">
		<GridView.ItemTemplate>
			<DataTemplate>
				<Border Margin="4" Width="150" Height="150" Background="#FFC8C8C8">
					<TextBlock Text="{Binding Album}" />
				</Border>
			</DataTemplate>
		</GridView.ItemTemplate>
	</GridView>
</UserControl>
