<UserControl
    x:Class="TunrRT.UserControls.TitleListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TunrRT.UserControls"
	xmlns:data="using:TunrRT.DataModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="480"
	d:DataContext="{d:DesignInstance Type=data:SampleTitleLibraryList, IsDesignTimeCreatable=True}">

	<UserControl.Resources>
		<local:TitleListConverter x:Key="TitleListConverter" />
	</UserControl.Resources>

	<ListView IsItemClickEnabled="True" ItemClick="ListView_ItemClick" ItemsSource="{Binding Results, Converter={StaticResource TitleListConverter}}">
		<ListView.ItemTemplate>
			<DataTemplate>
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="{Binding TrackNumber}" FontFamily="Segoe WP" FontWeight="Black" Foreground="Black" FontSize="20" Margin="0,0,0,4" VerticalAlignment="Bottom" />
					<TextBlock Text="{Binding Title}" FontFamily="Segoe WP" FontWeight="SemiLight" Foreground="Black" FontSize="30" Margin="6,0,0,0" VerticalAlignment="Bottom"/>
				</StackPanel>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</UserControl>
